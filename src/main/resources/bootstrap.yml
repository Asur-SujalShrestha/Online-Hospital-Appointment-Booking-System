spring:
  application:
    name: AppointmentBooking

  cloud:
    config:
      uri: http://config-server:8090
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 10
        multiplier: 1.5
